import { Router } from "express";

const router: Router = Router();

// router.post("/");

// router.get("/");

// router.get("/:id");

// router.patch("/:id");

// router.delete("/:id");

export { router as categoryRouter };
